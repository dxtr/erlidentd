{erl_opts, [debug_info]}.
{deps, []}.

{relx, [{release, {erlidentd, "0.1.0"},
         [erlidentd, sasl]},

        {dev_mode, true},
        {include_erts, false},

        {extended_start_script, true}]}.

{profile, [{prod, [{relx, [{dev_mode, false},
			{include_erts, true}]}]
	}]
}.
